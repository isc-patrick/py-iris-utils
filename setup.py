#!/usr/bin/env python

from distutils.core import setup

setup(name='py_iris_utils',
      version='1.0',
      description='Python IRIS utility library',
      packages=['py_iris_utils'],
      package_dir={"py_iris_utils": "py_iris_utils"},
      entry_points={
      },
      install_requires=[
    ]
     )